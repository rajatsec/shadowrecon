#!/usr/bin/env bash
# ShadowRecon launcher — uses the bundled venv and handles imports automatically.
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="$DIR/venv/bin/python"
[ -x "$PY" ] || PY="python3"
exec "$PY" "$DIR/main.py" "$@"
